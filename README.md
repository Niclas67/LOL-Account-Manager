## How to Install

Ideally, use a virtual environment (venv):

1. Install Python version 3.12.2  
   https://www.python.org/downloads/release/python-3122/

2. Open Command Prompt inside the `\lol_account_manager` directory and run:
   ```bash
   pip install -r requirements.txt
   ```

3. Execute:
   ```bash
   python DatabaseHandler.py
   ```

4. Start the program by double-clicking:
   ```
   Lol Account Manager.vbs
   ```

   You can also create a desktop shortcut for easier access.
      
